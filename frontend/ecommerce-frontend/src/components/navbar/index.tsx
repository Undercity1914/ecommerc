import { Button } from "../ui/button";

export default function Navbar(){
    return(
        <div>
            <Button variant={"ghost"} className="bg-black text-white">Pesquisar Produto</Button>
        </div>
    );
}